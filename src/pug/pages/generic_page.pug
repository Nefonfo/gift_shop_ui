extends ../templates/master.pug
include ../components/generic_hero.pug

block title
    title Productos

block content
    +generic_hero('Title', 'https://api.lorem.space/image/car?w=800&h=200&hash=8B7BCDC2')
    .w-full.flex.flex-wrap.justify-center.items-center.my-10.px-2
        .w-full.bg-primary-content.rounded-xl.shadow-card.py-8.px-10.leading-relaxed.transition-all.ease-in-out.duration-300(class='md:text-xl md:max-w-6xl')
            p Hola a todos,
            p
            p Lorem, ipsum dolor sit amet consectetur adipisicing elit. Assumenda consequatur accusantium sunt quasi beatae vel, iste animi aliquid cupiditate facere optio illum modi nemo laborum dignissimos numquam debitis ex cumque!

block extra_js
    script.
        document.addEventListener('DOMContentLoaded', function () {
            const paragraphs_empty = Array.from(document.querySelectorAll('p')).filter((paragraph) => paragraph.innerHTML.trim() === '')
            paragraphs_empty.forEach(paragraph => {
                const enter_block = document.createElement('br');
                paragraph.replaceWith(enter_block); 
            })
        });